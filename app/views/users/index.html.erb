<div class= 'users'>
<% @users.each do |user| %>
    <div class= 'user'>
    <h2><%= link_to user.name, user_path(user) %></h2> 
    <small>Number of posts: <%= user.postscounter %></small>
    </div>
<% end %>
</div>
